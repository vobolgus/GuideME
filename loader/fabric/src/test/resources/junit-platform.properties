# Enables ServiceLoader-based autodetection of JUnit Jupiter extensions so that
# BootstrapMinecraftExtension (META-INF/services in this directory) applies to the whole
# shared suite without touching the shared test sources. :fabric test classpath only.
junit.jupiter.extensions.autodetection.enabled=true
